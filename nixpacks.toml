[phases.install]
cmds = ['npm ci --no-audit --progress=false']

[phases.build]
cmds = ['npx prisma generate', 'npx prisma migrate deploy']

[start]
cmd = 'npm start'

[variables]
NODE_ENV = 'production'
PORT = '3000'
NIXPACKS_NODE_VERSION = '22.12.0'