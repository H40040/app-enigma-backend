[providers]
nodejs = { version = '22.12.0' }

[phases.install]
cmds = ['npm ci']

[phases.build]
cmds = ['npx prisma generate', 'npx prisma migrate deploy']

[start]
cmd = 'npm start'

[variables]
NODE_ENV = 'production'
PORT = '3000'