[phases.setup]
nixPkgs = ['nodejs-20', 'npm-10']

[phases.install]
cmds = ['npm ci']

[phases.build]
cmds = ['npx prisma generate', 'npx prisma migrate deploy']

[start]
cmd = 'npm start'

[variables]
NODE_ENV = 'production'
PORT = '3000'