[phases.setup]
nixPkgs = ['nodejs-20_x', 'npm-10_x']

[phases.install]
cmds = ['npm ci']

[phases.build]
cmds = ['npx prisma generate', 'npx prisma migrate deploy']

[start]
cmd = 'npm start'

[variables]
NODE_ENV = 'production'
PORT = 3000